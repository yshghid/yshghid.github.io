---
title: Tools
author: SH Youn
date: 2024-05-20
category: Jekyll
layout: post
---

툴 & 코드 정리  Code Archieve for Bioinformatics Tools # python # linux # R


Aligner
-------------

### Kallisto

#### 1) Install Kallisto

```bash
# conda install 로 설치
$ conda install kallisto
```

```bash
# git clone 으로 설치
$ git clone https://github.com/pachterlab/kallisto.git
```

```bash
# kallisto_linux-v0.46.1.tar.gz 파일 경로 설정해주기
$ vim ~/.bashrc
```
```bash
# $TOOLS 뒤에 :$KALLISTO 추가
KALLISTO=/data1/packages/kallisto
```
```bash
$ source ~/.bashrc
```

#### 2) Test

```bash
$ cd kallisto/test
$ kallisto index -i transcripts.idx transcripts.fasta.gz
```
```bash
$ kallisto quant -i transcripts.idx -o output -b 100 reads_1.fastq.gz reads_2.fastq.gz
or
$ kallisto quant -i transcripts.idx -o output -b 100 <(gzcat reads_1.fastq.gz) <(gzcat reads_2.fastq.gz)
```

#### 3) Building Kallisto index

gtf 파일로 생성 Building index with gtf reference file
```bash
# reference gtf file is in >> ../2307_EBV/hg38.refGene.gtf
$ kallisto index -i transcripts_gtf.idx ../2307_EBV/hg38.refGene.gtf
```
kallisto 에서 제공하는 유전체 파일로 생성 Building index with kallisto idx file
```bash
$ kallisto index -i transcripts.idx homo_sapiens.tar.gz
```

#### 4) 슈도얼라인 Pseudoalign
여기서 중요한 것은 cDNA index (transcripts_cDNA.idx)를 사용한 것. FAQ에 들어가보면 cDNA fasta 파일을 권장한다고 나와있다(We recommend using cDNA fasta, specifically the *.cdna.all.fa.gz files).
FAQ link - https://pachterlab.github.io/kallisto/faq

```bash
kallisto quant -i transcripts_cDNA.idx -o output_150-1 -t 40 -b 100 ../2306_tophat/data/Bowtie2Index/5-AZA_150-1_1_edited.fastq ../2306_tophat/data/Bowtie2Index/5-AZA_150-1_2_edited.fastq
```

abundance.h5 / abundance.tsv / run_info.json >> 3개 파일이 생성되었다면 성공!


### TopHat2

### Bismark

### RSubread

Differentially expression analysis
-------------

### edgeR

### DESeq2

```
library("DESeq2")
library(pheatmap)
library(withr)
#library(tidyverse)
library(RColorBrewer)
```
#### 1) Run DESeq2
```
cell <- "B"
S <- "NM" #subtype
S1 <- substr(S, 1, 1)
S2 <- substr(S, 2, 2)
setwd(paste0('/data/home/ysh980101/DEG/data4/', cell))
countdata <- read.csv(paste0('/data/home/ysh980101/DEG/data4/', cell, '/', cell, '_', S, '_lnc_0.8cutoff.csv'), header=TRUE, sep=',')
metadata <- read.csv(paste0('/data/home/ysh980101/DEG/data4/', cell, '/', cell, '.subtype_',S,'.csv'), header=TRUE, sep=',')
N <- dim(countdata)[[2]]
cData = countdata[,2:N]
GeneID = countdata[,1]
rownames(cData) = GeneID
print(paste0('cell-',cell, ' subtype-',S1, S2, ' dim(countdata)-',dim(countdata)[[1]],' ',dim(countdata)[[2]]))

dds <- DESeqDataSetFromMatrix(countData = cData, colData = metadata, design = ~subtype)
dds$subtype <- relevel(dds$subtype, ref = S1)

dds <- DESeq(dds)
res <- results(dds, contrast = c("subtype", S2, S1))
      
res_tbl <- res %>% data.frame() %>% rownames_to_column(var="gene") %>% as_tibble()

padj_cutoff <- 0.05
sig_res <- dplyr::filter(res_tbl, padj < padj_cutoff) %>% dplyr::arrange(padj)
#write_csv(sig_res, paste0('/data/home/ysh980101/DEG/data4/',cell,'/res_',S,'_pv0.05.csv'))
     
sig_res2 <-sig_res[abs(sig_res$log2FoldChange)>1,]
#write_csv(sig_res2, paste0('/data/home/ysh980101/DEG/data4/',cell,'/res_',S,'_pv0.05_lfc>1.csv'))
      
raw_counts <- counts(dds, normalized=FALSE)
sig_counts <- data.frame(raw_counts) %>% rownames_to_column(var = "gene") %>% dplyr::filter(gene %in% sig_res$gene)
#write_csv(sig_counts, paste0('/data/home/ysh980101/DEG/data4/',cell,'/sig_counts_',S,'.csv'))
      
sig_counts2 <- data.frame(raw_counts) %>% rownames_to_column(var = "gene") %>% dplyr::filter(gene %in% sig_res2$gene)
#write_csv(sig_counts2, paste0('/data/home/ysh980101/DEG/data4/',cell,'/sig_counts2_',S,'.csv'))
```

#### 2) Visualization with pheatmap
```
normalized_counts <- counts(dds,normalized = TRUE)
sig_norm <- data.frame(normalized_counts) %>% rownames_to_column(var = "gene") %>% dplyr::filter(gene %in% sig_res$gene)

ann_colors = list(subtype = setNames(c("#b0eb63", "#f7a1b5"), c(S1, S2)), celltype = setNames(c("#9BEDFB"),c(cell)))
heat_colors <- brewer.pal(6, "RdBu")
sig_norm2 <- sig_norm[, 2:length(colnames(sig_norm))]
df <- metadata[, c("subtype","celltype")]
rownames(df) <- colnames(sig_norm2)
rownames(sig_norm2) = sig_norm[['gene']]      
      
heatmap_object <- pheatmap(sig_norm2,
                           color = heat_colors,
                           clustering_distance_rows = "euclidean",
                           clustering_distance_cols = "euclidean",
                           clustering_method = "ward",
                           cluster_rows = TRUE, 
                           show_rownames = FALSE,
                           show_colnames = FALSE,
                           annotation_col = df[, c("subtype", "celltype")],
                           annotation_colors = ann_colors,
                           border_color = NA,
                           fontsize = 10, 
                           scale = "row", 
                           treeheight_row = 100,
                           treeheight_col = 100,
                           width = 10,
                           height = 10,
                           filename = paste0('/data/home/ysh980101/DEG/data4/',cell,'/heatmap_',S,'.png'))
```
![image](https://github.com/yshghid/yshghid.github.io/assets/153489198/19dbc97f-af56-4da1-ad3f-9b5d650a928d)


row를 둘로 나눠서 시각화하기 Visualize by dividing rows into two:
```
heatmap_object <- pheatmap(sig_norm2,
                           color = heat_colors,
                           ...
                           cutree_rows = 2)
```
![image](https://github.com/yshghid/yshghid.github.io/assets/153489198/407b2d4d-981b-4bf9-8c6e-55ff3aa59404)



#### 3) 히트맵 상의 덴드로그램을 csv로 저장 Get dendrogram information into csv

히트맵을 rds로 저장
```
heatmap_object <- pheatmap(sig_norm2,
                           ...)
saveRDS(heatmap_object, paste0("/data/home/ysh980101/DEG/data4/",cell,"/heatmap_",cell,"_",S,".rds"))

row_order <- heatmap_object$tree_row$order
write.table(row_order, file = paste0('/data/home/ysh980101/DEG/data4/',cell,'/heatmap_',S,'_roworder.txt'))
      
row_labels <- heatmap_object$tree_row$labels
write.table(row_labels, file = paste0('/data/home/ysh980101/DEG/data4/',cell,'/heatmap_',S,'_rowlabels.txt'))
      
rownames <- rownames(sig_norm2)[row_order]
write.table(rownames, file = paste0('/data/home/ysh980101/DEG/data4/',cell,'/heatmap_',S,'_row.txt'))
```
dendextend 로 읽기
```
library('readr')
library('dendextend')
library(ape)

setwd("/Users/yshmbid/Desktop/rds")
heatmap_object <- readRDS(paste0("heatmap_",cell,"_",S,".rds"))
dend <- as.dendrogram(as.phylo(heatmap_object$tree_row))
png(paste0("dendrogram_", cell, "_", S, ".png"), width = 1600, height = 600)
plot(dend)
dev.off()

cut=2
cut_dend <- cutree(dend, k=cut)
cut_dend_df <- as.data.frame(cut_dend)
write.csv(cut_dend_df, file = paste0("cut_dend",cut,"_df_",cell,"_",S,".csv"))
```

#### 4) gprofiler 돌린 후 ggplot2 로 시각화 Visualize gprofiler data with ggplot2:

```
cl <- "1"

df <- read.csv(paste0("/data/home/ysh980101/DEG/data4/", cell, "/pathways_", cell, "_", S, "_c", cl,".csv"), header=TRUE, sep=',')
df_pathway <- df[, c('pathway','genes','p.adjust','p.value')]
df_pathway <- df_pathway[df_pathway$p.adjust<0.05, ]
df_pathway$reg_type <- ifelse(df_pathway$p.adjust<0, "Up", "Down")  # above / below avg flag
df_pathway$log_padj <- -log10(df_pathway$p.adjust)
df_pathway <- df_pathway[order(df_pathway$log_padj), ]
df_pathway$pathway <- factor(df_pathway$pathway, levels = df_pathway$pathway)
bar_plot <- ggplot(df_pathway, aes(x=pathway, y=log_padj, label=log_padj)) +
    geom_bar(stat='identity', aes(fill=reg_type), width=.7)  +
    scale_fill_manual(name="Expression", labels = c("Up", "Down"),
    values = c("Down"="#00ba38", "Up"="#f8766d")) +
    labs(subtitle=paste0(cell," ",S," / cluster 2 (p-adj<0.05)"),
    title= "Pathway enrichment") +
    coord_flip()
ggsave(filename = paste0("/data/home/ysh980101/DEG/data4/", cell, "/pathways_", cell, "_", S, "_c", cl, ".png"), plot = bar_plot, width = 8, height = 10, dpi = 100)
dev.off()
```
![image](https://github.com/yshghid/yshghid.github.io/assets/153489198/4d734464-36cc-45db-b0af-564a1ffa5690)



### Sleuth

#### 1) Install Sleuth

```bash
if (!requireNamespace("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install()
BiocManager::install("devtools")    
BiocManager::install("pachterlab/sleuth")
```

#### 2) Run Sleuth

R script 를 사용하였음

참고 링크 - https://pachterlab.github.io/sleuth_walkthroughs/pval_agg/analysis.html

```
require("sleuth")
packageVersion("sleuth")
library("gridExtra")
library("cowplot")
library("biomaRt")
library(readr)

setwd("/data/home/ysh980101/2307_kallisto")
getwd()

sample_id <- dir(file.path("./"))
sample_id <- grep("^output_(150|con)", sample_id, value = TRUE)
sample_id <- substring(sample_id, 8)
sample_id

kal_dirs <- file.path(paste0("./output_", sample_id))
s2c <- read.table(file.path("./kallisto_demo_150_con.tsv"),
                  header = TRUE,
                  stringsAsFactors = FALSE,
                  sep = "\t")
s2c <- dplyr::mutate(s2c, path = kal_dirs)
s2c

marts <- listMarts()
ensembl <- useMart("ensembl")
datasets <- listDatasets(ensembl)
filtered_datasets <- datasets[grepl("hsapiens", datasets$dataset), ]
hsapiens_mart <- useMart("ensembl",dataset="hsapiens_gene_ensembl")
datasets <- listDatasets(hsapiens_mart)
filtered_datasets <- datasets[grepl("hsapiens", datasets$dataset), ]
hsapiens_mart <- useMart("ensembl",dataset="hsapiens_gene_ensembl",host="ensembl.org")
datasets <- listDatasets(hsapiens_mart)

t2g <- getBM(attributes = c("ensembl_transcript_id_version",
                            "ensembl_gene_id",
                            "description",
                            "external_gene_name"),
             mart = hsapiens_mart)
head(t2g)

ttg <- dplyr::rename(t2g, target_id= ensembl_transcript_id_version, 
                     ens_gene = ensembl_gene_id, ext_gene = external_gene_name)
ttg <- dplyr::select(ttg, c('target_id', 'ens_gene', 'ext_gene'))
head(ttg)

s2c$condition <- as.factor(s2c$condition)
s2c$condition <- relevel(s2c$condition, ref = "con")
so <- sleuth_prep(s2c, target_mapping = ttg,
  aggregation_column = 'ens_gene', extra_bootstrap_summary = TRUE)

so <- sleuth_fit(so, ~condition, 'full')
so <- sleuth_fit(so, ~1, 'reduced')
so <- sleuth_lrt(so, 'reduced', 'full')
sleuth_table_gene <- sleuth_results(so, 'reduced:full', 'lrt', show_all = FALSE)

sleuth_save(so, './sleuth_ward/150_con_so.sleuth')
write_csv(sleuth_table_gene, './sleuth_ward/150_con.csv')
```


PCA 그려주는 코드도 있으나 서버가 자꾸 꺼져서 생략하였음 There is code available to draw PCA plots, it has been omitted because the server keeps shutting down:

```
#PCA 코드
#1 so 생성
> so <- sleuth_prep(s2c, target_mapping = ttg,
+   aggregation_column = 'ens_gene', extra_bootstrap_summary = TRUE)

#2 PCA plot
> library(cowplot)
> ggplot2::theme_set(theme_cowplot())
> plot_pca(so, color_by = 'condition', text_labels = TRUE)
```
<img width="262" alt="image" src="https://github.com/yshghid/yshghid.github.io/assets/153489198/442f4087-cf08-4e94-a7c8-597e31aa0dd8">



위 코드에서는 통계 테스트로 'lrt'를 사용하였는데 이 경우 차등 발현의 크기가 제공되지 않음. 차등 발현의 크기도 제공되는 test type을 사용하려면 ward test 사용하기. In the above code, 'lrt' was used for statistical testing, but in this case, the magnitude of differential expression is not provided. To use a test type that also provides the magnitude of differential expression, use the ward test:

```
#sleuth_prep까지 동일 #same until sleuth_prep

so <- sleuth_fit(so, ~condition, 'full')
so <- sleuth_fit(so, ~1, 'reduced')
so <- sleuth_wt(so, 'condition150')
sleuth_table_gene <- sleuth_results(so, 'condition150', 'wald', show_all = FALSE)
sleuth_save(so, './sleuth_ward/150_con_so.sleuth')
write_csv(sleuth_table_gene, './sleuth_ward/150_con.csv')
```


gprofiler 돌린 후 ggplot2 로 시각화 Visualize gprofiler data with ggplot2:

```
library(ggplot2)
setwd("/data/home/ysh980101/2307_kallisto")
getwd()

condition <- '150_con'
#gpsource <- 'GO:BP'
gpsource <- 'REAC'

df_c1 <- read.csv(paste0("./sleuth_ward/gprofiler/gProfiler_",condition,"_termsize.csv"))
df_c2 <- read.csv(paste0("gProfiler_",condition,"_c2_padj0.1.csv"))

df_c1 <- df_c1[df_c1$source == gpsource, ]
df_c2 <- df_c2[df_c2$source == gpsource, ]
df_c1$reg_type <- 'down'
df_c2$reg_type <- 'up'
df_c1$nlog <- -abs(df_c1$negative_log10_of_adjusted_p_value)
df_c2$nlog <- abs(df_c2$negative_log10_of_adjusted_p_value)
df_c1 <- df_c1[order(df_c1$negative_log10_of_adjusted_p_value), ]
df_c2 <- df_c2[order(-df_c2$negative_log10_of_adjusted_p_value), ]
df <- rbind(df_c1, df_c2)
ggplot(df, aes(x = reorder(term_name, nlog), y = negative_log10_of_adjusted_p_value, size = intersection_size, color = nlog)) +
  geom_point(alpha = 0.6) +
  theme(axis.text.y = element_text(angle = 0, vjust = 0.5, hjust=1)) +
  labs(title = "Bubble Plot - GO:BP / 150_con",
       x = "Term",
       y = "-log10(p-adj)",
       size = "Intersection Size",
       color = "-log10(p-adj)") +
  scale_size(range = c(1,10)) +
  scale_color_gradient2(low = "blue", mid = "white", high = "red") +
  coord_flip()
ggsave(filename = "./bubble_plot_150_con.png", width = 12, height = 6)
```
![image](https://github.com/yshghid/yshghid.github.io/assets/153489198/d9ddc069-3bdf-40b2-8763-bd68a5afcb78)



### BSseq

### methylkit

RNA-seq processing
-------------

### Seurat

### HTSeq

### Samtools

