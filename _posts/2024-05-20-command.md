---
title: Command
author: SH Youn
date: 2024-05-20
category: Posts
layout: post
---

자주 쓰는 커맨드 모음 Command Archieve

백그라운드로 돌리기 Running in background
-------------

```bash
tophat -o ./tophat_out_con-3 hg38.fa 5-AZA_con-3_1.fastq 5-AZA_con-3_2.fastq & 
bg
disown-h

# tophat 사용해서 RNA-seq 얼라인 할 때 시간 너무 오래걸려서 썼다.
# 터미널을 끄거나 맥북 덮어도 진행 되고, 터미널에 진행 상태는 뜨지 않음. 
# nohup이 보통 쓰는거라고 하는데 이상하게 안먹어서 썼다.
```

서버 명령어 Server 
-------------

```bash
# 실행중인 프로세스 확인 Checking the currently running processes
ps -ef|grep "tophat_out_33-1"
ps -u ysh980101
```

```bash
# 세션 죽이기 Killing the session
kill -9 세션번호
```

tmux
-------------

```bash
# 세션 종료 End session
$ Ctrl + d
```
```bash
# 세션 이름 지정하기 Assign session name
$ tmux new -s [세션명]
```
```bash
# 세션 종료하지 않고 기존 shell로 이동 Move to the previous shell without ending the session
$ (Ctrl + b) + d
```
```bash
# 세션 불러오기 Retrieve session
$ tmux ls
$ tmux attach -t [세션명]
```

RData
-------------

```R
# 저장
# save( 저장할 데이터명, file = "저장 경로\\저장할 데이터의 파일명.RData" )
save(work_data, file = "C:\\Users\\Hyejin\\Desktop\\work_data.RData")
```
```R
# 읽기
# load( file = "경로\\파일명.RData" )
load(file="C:\\Users\\Hyejin\\Desktop\\work_data.RData")
```

주피터랩 Jupyter lab
-------------

```bash
cd .jupyter/
rm -rf nohup.out
nohup jupyter lab jupyter_lab_config.py &
```

bashrc
-------------

```bash
vi .bashrc
source .bashrc
```
